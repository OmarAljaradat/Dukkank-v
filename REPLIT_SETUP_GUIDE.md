# 🚀 دليل تشغيل متجر دُكانك على Replit (خطوة بخطوة)

> هذا الدليل المباشر يشرح لك بالضبط كيف ترفع وتشغّل موقعك على Replit بـ 3 خطوات بسيطة فقط بدون أي كود!

---

## 📋 الخطوة 1: رفع المشروع إلى Replit

1. قم بإنشاء **Repl** جديد على موقع [Replit.com](https://replit.com).
2. اختر **Import from GitHub** أو قم بضغط ملفات المشروع ورفعها (Upload Folder).
3. اختر اللغات تلقائياً: **Node.js / Vite / TypeScript**.

---

## 🔒 الخطوة 2: إضافة متغيرات البيئة (Secrets)

في Replit، اضغط على أيقونة **Tools** على اليسار ثم اختر **Secrets 🔒** واضف المفاتيح التالية:

| Key (اسم المتغير) | Value (القيمة) | الوصف |
|------------------|---------------|-------|
| `JWT_SECRET` | `dukkank_jwt_secure_secret_key_2026_x89a` | مفتاح تشفير التوكنات (أمان عالي) |
| `ADMIN_EMAIL` | `admin@dukkank.com` | بريد دخول لوحة الأدمن |
| `ADMIN_PASSWORD` | `admin123` | كلمة مرور دخول الأدمن |
| `RESEND_API_KEY` | `re_123456789...` | مفتاح Resend لإرسال إيميلات الـ OTP للعملاء |
| `DATABASE_URL` | `postgresql://...` | (اختياري) رابط قاعدة بيانات PostgreSQL |

> 💡 **ملاحظة:** إذا لم تقم بإضافة `DATABASE_URL` فإن السيرفر سيعمل تلقائياً بحالة **In-Memory Fallback** فائقة السرعة بدون أي مشاكل!

---

## ▶️ الخطوة 3: تشغيل المتجر

1. اضغط زر **Run ▶️** الكبير في أعلى شاشة Replit.
2. سيقوم Replit بتثبيت المكتبات وتثبيت `pnpm` وتشغيل السيرفر والموقع تلقائياً!
3. سيظهر لك رابط الموقع المباشر في النافذة الجانبية (Webview).

---

## 🔐 بيانات لوحة الأدمن الافتراضية
- **الرابط:** `/admin/login`
- **البريد:** `admin@dukkank.com`
- **كلمة المرور:** `admin123`

---

✨ **مبروك! متجرك الآن يعمل بأعلى معايير الأمان والسرعة عالمياً!**
