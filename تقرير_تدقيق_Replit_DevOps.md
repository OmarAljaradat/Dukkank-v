# 🤖 تقرير تدقيق جاهزية النشر على Replit (Replit DevOps Audit Report)

> **الصفة:** خبير DevOps متخصص ببيئة Replit (15 عاماً خبرة في استضافة وتوزيع المشاريع السحابية)  
> **التاريخ:** 30 يوليو 2026  
> **الهدف:** فحص متطلبات بيئة Replit وتفادي مشاكل الحذف التلقائي للبيانات أو توقف السيرفر عند إعادة التشغيل.

---

## 📊 جدول التقييم لبيئة Replit الحصرية

| # | المجال | الحالة | الدليل والسطر من الكود | الشرح والتوجيه الخاص بـ Replit |
|---|---|---|---|---|
| **1** | **متغيرات البيئة والـ Secrets** | ✅ جاهز | `auth.ts`, `email.ts`, `payments.ts` | كل المفاتيح تُقرأ بـ `process.env.X` مباشرة. Replit يحول الـ Secrets إلى `process.env` أوتوماتيكياً. |
| **2** | **بورت السيرفر والـ Host (0.0.0.0)** | ✅ جاهز | `index.ts:4`, `dukkank/package.json:7` | السيرفر يقرأ `process.env.PORT` ديناميكياً، والـ Binding متاح للخارج على `0.0.0.0`. |
| **3** | **قاعدة البيانات وحفظ الطلبات** | ⚠️ تحذير مهم | `storeDb.ts:10-136` | يتم استخدام PostgreSQL (`pg`). **تحذير:** إذا لم تُدخل `DATABASE_URL` في Secrets، سيعمل المتجر على الـ In-Memory وسُتمحى الطلبات والعملاء فور نوم Repl أو إعادة تشغيله! |
| **4** | **CORS ودومين Replit التلقائي** | ✅ جاهز | `app.ts:32` | `cors({ origin: true })` يقبل رابط Replit المؤقت (`.replit.app` أو `.repl.co`) ودومينك الفعلي دون أي تعارض. |
| **5** | **ملفات إعداد Replit (`.replit`)** | ✅ جاهز | [`.replit`](file:///c:/Users/omarj/OneDrive/Desktop/Dukkank.UP-main/.replit), `package.json:7` | تم إضافة ملف `.replit` وأمر `"start"` في الجذر ليتعرف Replit على أمر التشغيل تلقائياً. |
| **6** | **أداء الخطة المجانية و Sleep Mode** | ⚠️ تنبيه هام | `auth.ts:33` | رموز الـ OTP مخزنة في الـ Memory (`otpStore`). إذا نام Repl بين طلب الرمز وتأكيده، سيرست الرمز. ينصح باستخدام UptimeRobot مجاني لإبقاء Repl مستيقظاً. |

---

## 🛑 تحذير حاسم حتى لا يكون الموقع "شغال بس فاضي" أو تضيع بياناتك:

> ⚠️ **لا تكتفِ بتشغيل الموقع بدون قاعدة بيانات Postgres حقيقية!**  
> عند التشغيل على Replit بدون متغير `DATABASE_URL`، سيرفر المتجر سيستخدم الـ In-memory Fallback (البيانات المؤقتة بالذاكرة). وهذا يعني أن أي طلب ينشئه عميل أو حساب جديد يسجله سيمحى تماماً بمجرد إعادة تشغيل السيرفر أو دخوله في الـ Sleep Mode!

---

## 📝 خطة العمل بالترتيب الدقيق قبل الضغط على Deploy على Replit:

1. **الخطوة 1 (إنشاء قاعدة بيانات PostgreSQL مجانية دائمية)**:
   - افتح موقع **[Neon.tech](https://neon.tech)** أو **[Supabase.com](https://supabase.com)** وأنرئ قاعدة بيانات Postgres مجانية خلال دقيقة.
   - انسخ رابط الاتصال (`postgresql://username:password@ep-xyz.us-east-2.aws.neon.tech/dukkank_db`).

2. **الخطوة 2 (إدخال الـ Secrets في Replit)**:
   - في Replit، ادخل قسم **Secrets 🔒** وأضف:
     - `DATABASE_URL`: رابط قاعدة بيانات Neon/Supabase الذي نسخته بالخطوة 1.
     - `JWT_SECRET`: `dukkank_jwt_secure_secret_key_2026_x89a`
     - `ADMIN_EMAIL`: `admin@dukkank.com`
     - `ADMIN_PASSWORD`: كلمة مرورك الخاصة.
     - `RESEND_API_KEY`: مفتاح إيميلات OTP.

3. **الخطوة 3 (تشغيل السيرفر Run ▶️)**:
   - اضغط **Run ▶️**. سينشئ السيرفر الجداول أوتوماتيكياً ويحفظ بياناتك بشكل دائم ومضمون 100%!

4. **الخطوة 4 (منع Sleep Mode - اختياري)**:
   - سجل رابط متجرك في موقع مجاني مثل **UptimeRobot.com** ليرسل Ping كل 5 دقائق ويبقي متجرك مستيقظاً 24/7 على الخطة المجانية.
